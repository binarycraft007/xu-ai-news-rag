# Flask Configuration
FLASK_APP=src.backend.app
FLASK_ENV=development
SECRET_KEY='a-super-secret-key' # Change this in production
JWT_SECRET_KEY='another-super-secret-key' # Change this in production

# Database Configuration
SQLALCHEMY_DATABASE_URI='sqlite:///project.db'

# Llama Server URLs
LLAMA_SERVER_LLM_URL='http://127.0.0.1:8080'
LLAMA_SERVER_EMBEDDING_URL='http://127.0.0.1:8081'
LLAMA_SERVER_RERANKING_URL='http://127.0.0.1:8082'

# Bing Search API Key (Optional)
# Get a key from the Microsoft Azure portal to enable Bing search fallback.
BING_API_KEY='your-bing-api-key'

# Email Notification Configuration (Optional)
# Used to send notifications when new documents are added.
MAIL_SERVER='smtp.example.com'
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME='your-email@example.com'
MAIL_PASSWORD='your-email-password'
